<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NexusConnect"
        tools:targetApi="31">

        <!-- Main config activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.NexusConnect">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Chooser dialog shown when user taps a widget tile -->
        <activity
            android:name=".widget.ContactChooserActivity"
            android:exported="false"
            android:theme="@style/Theme.NexusConnect"
            android:launchMode="singleTop" />

        <!-- AppWidget provider -->
        <receiver
            android:name=".widget.ContactWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.toxictrace.nexusconnect.ACTION_CONTACT_CLICK" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- RemoteViewsService for widget grid tiles -->
        <service
            android:name=".widget.ContactWidgetService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <!-- Contacts change observer -->
        <service
            android:name=".widget.ContactsObserverService"
            android:exported="false" />

    </application>

</manifest>
